<!DOCTYPE html>
<html>
  <head>
    <title>Edit: <%= @page.title %> - ActiveCanvas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">

    <!-- GrapeJS Core - load first before any AMD loaders -->
    <link rel="stylesheet" href="https://unpkg.com/grapesjs@0.21.13/dist/css/grapes.min.css">
    <script src="https://unpkg.com/grapesjs@0.21.13/dist/grapes.min.js"></script>

    <!-- GrapeJS Plugins -->
    <script src="https://unpkg.com/grapesjs-blocks-basic@1.0.2/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-plugin-forms@2.0.6/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-preset-webpage@1.0.3/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-style-bg@2.0.2/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-tabs@1.0.6/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-custom-code@1.0.2/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-touch@0.1.1/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-parser-postcss@1.0.3/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-tooltip@0.1.8/dist/index.min.js"></script>
    <script src="https://unpkg.com/grapesjs-typed@2.0.1/dist/index.min.js"></script>

    <!-- Monaco Editor - load after GrapeJS to avoid AMD conflicts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.45.0/min/vs/loader.min.js"></script>
    <script>
      require.config({ paths: { vs: 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.45.0/min/vs' } });
      window.MonacoEnvironment = {
        getWorkerUrl: function() {
          return URL.createObjectURL(new Blob([`
            self.MonacoEnvironment = {
              baseUrl: 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.45.0/min'
            };
            importScripts('https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.45.0/min/vs/base/worker/workerMain.js');
          `], { type: 'text/javascript' }));
        }
      };
    </script>

    <style>
      :root {
        --editor-bg: #1a1a1a;
        --editor-panel: #2d2d2d;
        --editor-border: #3d3d3d;
        --editor-text: #e5e5e5;
        --editor-text-muted: #999;
        --editor-primary: #3b82f6;
        --editor-primary-dark: #2563eb;
        --editor-success: #22c55e;
        --editor-danger: #ef4444;
      }

      * { box-sizing: border-box; margin: 0; padding: 0; }

      html, body {
        height: 100%;
        overflow: hidden;
      }

      body {
        font-family: system-ui, -apple-system, sans-serif;
        background: var(--editor-bg);
        color: var(--editor-text);
      }

      /* Editor Header */
      .editor-header {
        height: 50px;
        background: var(--editor-panel);
        border-bottom: 1px solid var(--editor-border);
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 1rem;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
      }

      .editor-header-left {
        display: flex;
        align-items: center;
        gap: 1rem;
      }

      .editor-back {
        color: var(--editor-text-muted);
        text-decoration: none;
        font-size: 0.875rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .editor-back:hover {
        color: var(--editor-text);
      }

      .editor-title {
        font-size: 0.875rem;
        font-weight: 500;
        color: var(--editor-text);
        border-left: 1px solid var(--editor-border);
        padding-left: 1rem;
      }

      .editor-header-center {
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .editor-header-right {
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      /* Device Buttons */
      .device-btn {
        background: transparent;
        border: none;
        color: var(--editor-text-muted);
        padding: 0.5rem;
        border-radius: 0.25rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .device-btn:hover,
      .device-btn.active {
        background: var(--editor-bg);
        color: var(--editor-text);
      }

      .device-btn svg {
        width: 20px;
        height: 20px;
      }

      /* Header Buttons */
      .header-btn {
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        font-size: 0.8125rem;
        font-weight: 500;
        cursor: pointer;
        border: none;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .header-btn-secondary {
        background: var(--editor-bg);
        color: var(--editor-text);
        border: 1px solid var(--editor-border);
      }

      .header-btn-secondary:hover {
        background: var(--editor-border);
      }

      .header-btn-primary {
        background: var(--editor-primary);
        color: white;
      }

      .header-btn-primary:hover {
        background: var(--editor-primary-dark);
      }

      .header-btn-primary:disabled {
        opacity: 0.5;
        cursor: not-allowed;
      }

      /* Main Editor Container */
      .editor-container {
        display: flex;
        flex: 1;
        width: 100%;
        overflow: hidden;
      }

      /* Left Panel - Blocks */
      .editor-panel-left {
        width: 240px;
        min-width: 240px;
        background: var(--editor-panel);
        border-right: 1px solid var(--editor-border);
        display: flex;
        flex-direction: column;
        overflow: hidden;
        flex-shrink: 0;
      }

      .panel-tabs {
        display: flex;
        border-bottom: 1px solid var(--editor-border);
      }

      .panel-tab {
        flex: 1;
        padding: 0.75rem;
        background: transparent;
        border: none;
        color: var(--editor-text-muted);
        font-size: 0.75rem;
        font-weight: 500;
        text-transform: uppercase;
        cursor: pointer;
        border-bottom: 2px solid transparent;
      }

      .panel-tab:hover {
        color: var(--editor-text);
      }

      .panel-tab.active {
        color: var(--editor-primary);
        border-bottom-color: var(--editor-primary);
      }

      .panel-content {
        flex: 1;
        overflow-y: auto;
        padding: 0.5rem;
      }

      /* Canvas */
      .editor-canvas {
        flex: 1;
        min-width: 0;
        background: var(--editor-bg);
        position: relative;
        overflow: hidden;
      }

      #gjs {
        height: 100%;
        width: 100%;
      }

      /* Force GrapeJS frame to fill container */
      .gjs-cv-canvas {
        width: 100% !important;
        height: 100% !important;
      }

      .gjs-frame-wrapper {
        width: 100% !important;
      }

      /* Right Panel - Styles/Layers */
      .editor-panel-right {
        width: 280px;
        min-width: 280px;
        background: var(--editor-panel);
        border-left: 1px solid var(--editor-border);
        display: flex;
        flex-direction: column;
        overflow: hidden;
        flex-shrink: 0;
      }

      /* GrapeJS Overrides */
      .gjs-one-bg {
        background-color: var(--editor-panel);
      }

      .gjs-two-color {
        color: var(--editor-text);
      }

      .gjs-three-bg {
        background-color: var(--editor-bg);
      }

      .gjs-four-color,
      .gjs-four-color-h:hover {
        color: var(--editor-primary);
      }

      /* Blocks */
      .gjs-block {
        width: calc(50% - 4px);
        min-height: 60px;
        padding: 0.5rem;
        margin: 2px;
        border-radius: 0.375rem;
        font-size: 0.6875rem;
      }

      .gjs-block:hover {
        box-shadow: 0 0 0 2px var(--editor-primary);
      }

      .gjs-block-label {
        font-size: 0.6875rem;
      }

      .gjs-block-category .gjs-title {
        font-size: 0.75rem;
        padding: 0.75rem 0.5rem;
        border-bottom: 1px solid var(--editor-border);
      }

      /* Layers */
      .gjs-layers {
        padding: 0.5rem;
      }

      .gjs-layer {
        font-size: 0.75rem;
      }

      /* Style Manager */
      .gjs-sm-sector {
        border-bottom: 1px solid var(--editor-border);
      }

      .gjs-sm-sector-title {
        font-size: 0.75rem;
        padding: 0.75rem 0.5rem;
        font-weight: 600;
      }

      .gjs-sm-properties {
        padding: 0.5rem;
      }

      .gjs-sm-label {
        font-size: 0.6875rem;
      }

      .gjs-field {
        background: var(--editor-bg);
        border: 1px solid var(--editor-border);
        border-radius: 0.25rem;
      }

      .gjs-field input,
      .gjs-field select {
        font-size: 0.75rem;
        color: var(--editor-text);
      }

      /* Traits (Component Settings) */
      .gjs-trt-traits {
        padding: 0.5rem;
      }

      .gjs-trt-trait {
        margin-bottom: 0.5rem;
      }

      .gjs-trt-trait .gjs-label {
        font-size: 0.6875rem;
        margin-bottom: 0.25rem;
      }

      /* Selected Component */
      .gjs-selected {
        outline: 2px solid var(--editor-primary) !important;
        outline-offset: -2px;
      }

      .gjs-toolbar {
        background: var(--editor-primary);
      }

      .gjs-toolbar-item {
        padding: 0.25rem 0.5rem;
        font-size: 0.75rem;
      }

      /* Rich Text Editor */
      .gjs-rte-toolbar {
        background: var(--editor-panel);
        border: 1px solid var(--editor-border);
        border-radius: 0.375rem;
      }

      .gjs-rte-action {
        color: var(--editor-text);
        font-size: 0.875rem;
        padding: 0.5rem;
      }

      .gjs-rte-action:hover {
        background: var(--editor-bg);
      }

      /* Canvas Frame */
      .gjs-frame-wrapper {
        background: #fff;
      }

      /* Modal */
      .gjs-mdl-dialog {
        background: var(--editor-panel);
        border-radius: 0.5rem;
        max-width: 90vw;
        width: 1000px;
      }

      .gjs-mdl-header {
        border-bottom: 1px solid var(--editor-border);
      }

      .gjs-mdl-title {
        font-size: 0.875rem;
        font-weight: 600;
      }

      /* Asset Manager */
      .gjs-am-assets-cont {
        padding: 1rem;
      }

      .gjs-am-asset {
        border-radius: 0.375rem;
        margin: 0.25rem;
      }

      .gjs-am-asset-image {
        border-radius: 0.375rem 0.375rem 0 0;
      }

      .gjs-am-meta {
        padding: 0.5rem;
        font-size: 0.6875rem;
      }

      /* Code Editor Modal - styles defined in editor view for Monaco */
      .gjs-mdl-content {
        padding: 0;
      }

      /* Notification Toast */
      .editor-toast {
        position: fixed;
        bottom: 1rem;
        right: 1rem;
        padding: 0.75rem 1rem;
        border-radius: 0.375rem;
        font-size: 0.8125rem;
        font-weight: 500;
        z-index: 1000;
        display: none;
        animation: slideIn 0.3s ease;
      }

      .editor-toast.show {
        display: block;
      }

      .editor-toast.success {
        background: var(--editor-success);
        color: white;
      }

      .editor-toast.error {
        background: var(--editor-danger);
        color: white;
      }

      @keyframes slideIn {
        from {
          transform: translateY(100%);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }

      /* Loading Overlay */
      .editor-loading {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.7);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1000;
      }

      .editor-loading.hidden {
        display: none;
      }

      .editor-loading-spinner {
        width: 40px;
        height: 40px;
        border: 3px solid var(--editor-border);
        border-top-color: var(--editor-primary);
        border-radius: 50%;
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        to { transform: rotate(360deg); }
      }

      /* Scrollbar */
      ::-webkit-scrollbar {
        width: 8px;
        height: 8px;
      }

      ::-webkit-scrollbar-track {
        background: var(--editor-bg);
      }

      ::-webkit-scrollbar-thumb {
        background: var(--editor-border);
        border-radius: 4px;
      }

      ::-webkit-scrollbar-thumb:hover {
        background: var(--editor-text-muted);
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
